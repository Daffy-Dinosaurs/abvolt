import React, {Component} from "react";
import {connect} from "react-redux";


//TODO: this component will be a render component only. It will be based on the
// redux state. If loading is set to true the spinner should be visible. This
// action will be conducted in various parts of the application.

/*
action => toggle loading
  toggle loadgin takes in boolean

reducer => loading reducer
  changes state to true or false

component => renders spinner based on prop
  first implemenation can be just a message to user
*/

export default Spinner extends Component {
  render() {
    <div>Spinner</div>
  }
}
